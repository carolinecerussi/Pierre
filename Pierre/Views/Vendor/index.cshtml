@{
  Layout = "_Layout";
}

@using Pierre.Models;

<h1>VENDORS!</h1>

@if (Model.Count == 0)
{
  <p>No Vendors</p>
}
<ul>
  @foreach (Vendor vendor in Model)
  {
    <li><a href="/vendor/@vendor.Id">@vendor.VendorName</a></li>
  }
<ul>
<a href='/vendor/new'>New Vendor</a>
